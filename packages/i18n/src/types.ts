export interface I18nTypes {
  app: {
    name: string;
  };
  actions: {
    save: string;
    cancel: string;
    delete: string;
    archive: string;
    restore: string;
    edit: string;
    view: string;
  };
}
